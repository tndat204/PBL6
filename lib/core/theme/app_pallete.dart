import 'package:flutter/material.dart';

class AppPallete {
  static const Color backgroundColor = Color(0xFFF0FDF4); // Light greenish background
  static const Color primaryColor = Color(0xFF0D9488); // Teal-600 for primary actions
  static const Color secondaryColor = Color(0xFF06B6D4); // Bright cyan for secondary actions
  static const Color accentColor = Color(0xFF06B6D4); // Bright cyan for accents
  static const Color textColor = Color(0xFF374151); // Dark gray for text
  static const Color mutedTextColor = Color(0xFF6B7280); // Lighter gray for muted text
  static const Color borderColor = Color(0xFFD1D5DB); // Light gray for borders
  static const Color inputBackgroundColor = Color(0xFFFFFFFF); // White for input fields
  static const Color errorColor = Color(0xFFB91C1C); // Red for errors
  static const Color successColor = Color(0xFF10B981); // Green for success
  static const Color transparentColor = Colors.transparent;
  static const Color whiteColor = Colors.white;
  
  static const List<Color> primaryGradient = [
    Color(0xFF0D9488), // Teal
    Color(0xFF06B6D4), // Cyan
  ];
  
  static const List<Color> backgroundGradient = [
    Color(0xFFF0FDF4), // Light green
    Color(0xFFFFFFFF), // White
  ];
}
